{
  "name": "aungpao-bot",
  "version": "1.0.0",
  "description": "If you're finding a angpao yes it's not here but there's sniping script here. try checking yarn.lock in the site you have refered from",
  "main": "bot.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "chalk": "^5.0.0",
    "node-fetch": "^3.2.0"
  }
}
